import { DolarTodayPipe } from './dolar-today.pipe';

describe('DolarTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new DolarTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
